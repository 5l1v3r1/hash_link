# Quick script to link those 5600 mode hashes

## Dependencies

If bundler isn't installed: ```gem install bundler```

Then:

    cd hash_link
    bundle install

## Usage

    ruby linker.rb <hash file> <cracked file>
